import 'package:flutter/material.dart';

class ClassListSearchScreen extends StatelessWidget {
  static const routeName = '/class-list-search';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("찾기"),
      ),
    );
  }
}
